version: 2

sources:
    - name: stripe
      database: dbt-tutorial-334816
      schema: bdt_dataset
      tables:
        - name: Stripe_payments
#          loaded_at_field: created  # field to that data refresh timestamp
#          freshness:
#            warn_after: { count: 12, period: hour }
#            error_after: { count: 24, period: hour }